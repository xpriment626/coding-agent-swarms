// exa binding — research over Exa's hosted MCP / REST. Research-side copy.
// Parallel of pi-coral/src/team-swarm/agent-bindings/exa.ts; allowed to diverge.
//
// Loaded inside the `bun -e` subprocess.
//
// API:
//   exa.search(query): Promise<Result[]>
//   exa.fetch(url): Promise<string>
//
// Reads EXA_API_KEY from env. Auth via x-api-key header.

export {};
