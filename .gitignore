.gradle
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
.idea/
.idea
.kotlin
out/
!**/src/main/**/out/

### Mac OS ###
.DS_Store

### Project-specific ###
.env
.playwright-mcp
coral-agent.dev.env
coral-server.log
.npmrc
agent_log.json

### Local design / research notes (gitignored — keep local) ###
docs/
deepseek-swarm/

### Scaffolder helpers that shouldn't live in git ###
# CLAUDE.md is per-user orientation; keep local unless explicitly shared
CLAUDE.md

### TS workspace ###
node_modules/
# Anchored to repo root only — the SvelteKit telemetry-fe app has a
# routes/runs/[id]/ directory that should be tracked.
/runs/
bun.lockb
